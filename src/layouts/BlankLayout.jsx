import React from 'react';
// import CopyBlock from '@/components/Class';

const Layout = ({ children }) => (
  <>
    <div>{children}</div>
    {/* <CopyBlock id={Date.now()} /> */}
  </>
);

export default Layout;
